server {
    listen {{ lighthouse_listen_port }};
    server_name {{ lighthouse_server_name }};

    access_log {{ lighthouse_access_log }};
    error_log {{ lighthouse_error_log }};

    root {{ lighthouse_root_dir }};
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
